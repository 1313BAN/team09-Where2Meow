<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.where2meow.user.repository.UserRepository">

    <select id="findByEmail" resultType="com.ssafy.where2meow.user.entity.User">
        SELECT 
            user_id as userId,
            BIN_TO_UUID(uuid) as uuid,
            name,
            nickname,
            email,
            password,
            phone,
            role,
            image,
            is_active as isActive
        FROM user 
        WHERE email = #{email} AND is_active = true
    </select>

    <select id="findById" resultType="com.ssafy.where2meow.user.entity.User">
        SELECT 
            user_id as userId,
            BIN_TO_UUID(uuid) as uuid,
            name,
            nickname,
            email,
            password,
            phone,
            role,
            image,
            is_active as isActive
        FROM user 
        WHERE user_id = #{userId} AND is_active = true
    </select>
    
    <!-- 더미 데이터 관련 쿼리 -->
    <insert id="insertUser" parameterType="com.ssafy.where2meow.user.entity.User">
        INSERT INTO user (uuid, name, nickname, email, password, phone, role, image, is_active)
        VALUES (UUID_TO_BIN(#{uuid}), #{name}, #{nickname}, #{email}, #{password}, #{phone}, #{role}, #{image}, #{isActive})
    </insert>
    
    <delete id="deleteAllUsers">
        DELETE FROM user
    </delete>
    
    <select id="findAllUsers" resultType="com.ssafy.where2meow.user.entity.User">
        SELECT 
            user_id as userId,
            BIN_TO_UUID(uuid) as uuid,
            name,
            nickname,
            email,
            password,
            phone,
            role,
            image,
            is_active as isActive
        FROM user
        ORDER BY user_id
    </select>
    
    <select id="countUsers" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

</mapper>
